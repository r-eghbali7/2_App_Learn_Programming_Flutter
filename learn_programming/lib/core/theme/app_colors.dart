// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color darkBg = Color(0xFF0F1115);
  static const Color surfaceColor = Color(0xFF1E222D);
  static const Color primaryOrange = Color(0xFFFF8C00);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color successGreen = Color(0xFF34D399);
  static const Color errorRed = Colors.redAccent;
  
  // شیشه‌ای و گرادیان‌ها
  static Color surfaceWithOpacity = const Color(0xFF1E222D).withValues(alpha: 0.9);
  static Color borderLight = Colors.white.withValues(alpha: 0.05);
}